## 概述

本 PR 提交 LinkGen 小程序的暗色主题统一、名片视觉统一、标签选中反馈和页面主题审计结果。所有改动来自独立分支 `codex/dark-theme`，请负责人审核后再决定是否合并。

## 主要修改

### 暗色主题

- 为实际注册的 13 个页面接入统一主题状态。
- 统一页面背景、卡片、输入框、弹层、底部操作栏和禁用状态。
- 修复暗色主题下白底白字、蓝底灰字对比不足等问题。
- 拆分文字变量与操作按钮变量，主按钮使用 `--action-bg` / `--action-text`。
- 保留亮色主题切换能力，并同步导航栏和 TabBar 样式。

### 交互反馈

- 标签、头像、分类等可选控件增加按下反馈。
- 标签选中后持续显示紫色背景、亮色边框、白色文字和 `✓`。
- 禁用按钮降低透明度，避免误认为可操作。

### 名片体验

- “我的”页面名片、编辑页实时预览、数字名片预览统一使用深蓝名片视觉。
- 橘色保留给活动封面、头像边框和活动强调，避免名片与活动卡片混淆。

### 文档与目录

- 新增 `docs/theme-audit.md`，记录 13 个实际页面的主题验收矩阵和新增页面规范。
- 将调试文件整理至 `debug/`，避免散落在工作区根目录。

## 验证

- `app.json` JSON 解析通过。
- 所有页面 JavaScript 语法检查通过。
- `git diff --check` 通过。
- 已在微信开发者工具实际查看发现页和活动页暗色渲染。

## 注意事项

- 本 PR 不包含本地测试 AppID 和本地开发者工具配置文件。
- 不包含 CloudBase 后端迁移；后端身份、权限、任务状态仍需后续单独设计。
- 请先审核，不要直接假设自动合并。
