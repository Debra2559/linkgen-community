const { getMembers } = require('../../utils/linkgen-data');
const { getTagTone } = require('../../utils/tag-taxonomy');
Page({ data: { member: null }, onLoad(options) { const member = getMembers().find((item) => item.id === options.id); this.setData({ member: member ? { ...member, tagItems: (member.tags || []).map((label) => ({ label, tone: getTagTone(label) })) } : null }); }, connect() { wx.showModal({ title: '交换微信', content: '发送好友申请前，请确认你们已经在活动或讨论中认识。', confirmText: '发送申请', success: (res) => { if (res.confirm) wx.showToast({ title: '申请已发送', icon: 'success' }); } }); }, sendMessage() { wx.showToast({ title: '私信功能即将开放', icon: 'none' }); } });
